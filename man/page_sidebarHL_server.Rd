% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_sidebarHL_server.R
\name{page_sidebarHL_server}
\alias{page_sidebarHL_server}
\title{Logique serveur de page_sidebarHL}
\usage{
page_sidebarHL_server(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{Objet session Shiny. Par defaut la session courante.}
}
\description{
A appeler une fois dans le \code{server} de l'application. Gere :
\itemize{
\item Le routing sidebar -> contenu (\code{nav_select()})
\item La mise a jour de l'URL a chaque navigation (\code{?page=valeur})
}
}
\details{
Lorsque utilise avec protegR2, appeler depuis
\code{protegR2_load_modules_servers()} en passant la session principale.
}
\section{Inputs Shiny exposes}{


Deux inputs sont disponibles cote serveur pour reagir a la navigation :

\strong{\code{input$hl__nav}} — declenche uniquement par un clic utilisateur dans la
sidebar. Vaut \code{NULL} au demarrage (avant tout clic). Utile pour reagir a
une action explicite de l'utilisateur :

\if{html}{\out{<div class="sourceCode r">}}\preformatted{observeEvent(input$hl__nav, \{
  # declenche seulement quand l'utilisateur clique
\})
}\if{html}{\out{</div>}}

\strong{\code{input$hl__content}} — expose automatiquement par bslib via
\code{navset_hidden(id = "hl__content")}. Initialise avec la page selectionnee
au demarrage, puis mis a jour a chaque navigation (clic OU \code{hl_nav_select()}).
A privilegier quand on a besoin de connaitre la page active a tout moment :

\if{html}{\out{<div class="sourceCode r">}}\preformatted{observe(\{
  page <- input$hl__content   # jamais NULL, valide des le demarrage
\})
}\if{html}{\out{</div>}}

Ces deux inputs sont distincts de \code{input$nav_tab} utilise par protegR2 pour
ses propres layouts — aucun conflit.
}

