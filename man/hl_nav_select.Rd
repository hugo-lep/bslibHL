% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_sidebarHL_server.R
\name{hl_nav_select}
\alias{hl_nav_select}
\title{Changer de page depuis le serveur}
\usage{
hl_nav_select(value, session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{value}{Valeur (\code{value}) du \code{\link[=hl_nav_panel]{hl_nav_panel()}} a activer.}

\item{session}{Objet session Shiny. Par defaut la session courante.}
}
\description{
Change la page active de \code{\link[=page_sidebarHL]{page_sidebarHL()}} depuis le code serveur, en
mettant a jour simultanement :
\itemize{
\item Le contenu affiche (\code{navset_hidden})
\item L'etat visuel actif dans la sidebar (classes CSS)
\item L'URL du navigateur (\code{?page=valeur})
}
}
\details{
A utiliser a la place d'un appel direct a \code{bslib::nav_select()} qui, lui,
ne mettrait pas a jour les classes CSS de la sidebar ni l'URL.

Apres appel, \code{input$hl__content} reflete la nouvelle page. \code{input$hl__nav}
ne se declenche pas (il est reserve aux clics utilisateur).
}
