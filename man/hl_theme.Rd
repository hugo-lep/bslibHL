% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hl_theme.R
\name{hl_theme}
\alias{hl_theme}
\title{Theme bslib standard de bslibHL}
\usage{
hl_theme(primary = "#3c8dbc", bootswatch = "darkly", ...)
}
\arguments{
\item{primary}{Couleur primaire (boutons sidebar, liens actifs, etc.).
Defaut : \code{"#3c8dbc"}.}

\item{bootswatch}{Theme Bootswatch de base. Defaut : \code{"darkly"}.}

\item{...}{Arguments supplementaires passes a \code{\link[bslib:bs_theme]{bslib::bs_theme()}}
(ex. \code{base_font}, \code{secondary}, etc.).}
}
\value{
Un objet \code{bs_theme} utilisable dans le parametre \code{theme} de
\code{\link[=page_sidebarHL]{page_sidebarHL()}} ou de n'importe quelle page bslib.
}
\description{
Cree un \code{\link[bslib:bs_theme]{bslib::bs_theme()}} pre-configure avec le look par defaut de
bslibHL. Sert de point de depart — tous les parametres de \code{\link[bslib:bs_theme]{bslib::bs_theme()}}
peuvent etre passes via \code{...} pour personnaliser davantage.
}
\examples{
\dontrun{
# Look standard
page_sidebarHL(theme = hl_theme(), ...)

# Variante avec une autre couleur primaire
page_sidebarHL(theme = hl_theme(primary = "#27ae60"), ...)

# Autre bootswatch
page_sidebarHL(theme = hl_theme(bootswatch = "flatly", primary = "#2c3e50"), ...)
}

}
